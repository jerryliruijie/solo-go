[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "solo-go"
version = "0.0.1"
description = "Solo Go trainer"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["goai"]
